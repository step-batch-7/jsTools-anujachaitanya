

    encoding: "utf8"
    process.stderr.write(contents.error);
    process.stdout.write(contents.message);
    reader: fs.readFile,
  const display = function(contents) {
  const fsTools = {
  sort(cmdLineArgs, fsTools, console.log, console.error);
  };
  };
const cmdLineArgs = process.argv.slice(2);
const fs = require("fs");
const main = function() {
const { sort } = require("./src/executeSort");
main();
};
